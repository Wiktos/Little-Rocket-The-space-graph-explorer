#pragma once

/*
	Struct ApplicationParameters represents basic parameters that are needed to launch an application.

	by Wiktor £azarski
*/
struct ApplicationParameters {
	int layers;
	int nodesPerLayer;
	int layersConnections;
};